#!/bin/bash

# Exit immediately if a command exits with a non-zero status
set -e


# Install NPM modules if required for web UI
echo "Installing NPM modules here"
# pushd ./src/web
# npm install
# npx next telemetry disable
# popd